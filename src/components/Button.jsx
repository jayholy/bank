import React from 'react'

const Button = ({styles}) => {
  return (
    <button type='button' className={`py-4 md:w-[10rem] w-full px-6 bg-blue-gradient font-poppins rounded-md mt-4 font-medium text-primary text-[18px] outline-none`}>
Get Started
    </button>
  )
}

export default Button